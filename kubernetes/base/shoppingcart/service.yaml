apiVersion: v1
kind: Service
metadata:
  name: mango-shoppingcart
  namespace: mango
spec:
  type: ClusterIP
  selector:
    app: mango-shoppingcart
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP