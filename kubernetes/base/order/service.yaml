apiVersion: v1
kind: Service
metadata:
  name: mango-order
  namespace: mango
spec:
  type: ClusterIP
  selector:
    app: mango-order
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP