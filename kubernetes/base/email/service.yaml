apiVersion: v1
kind: Service
metadata:
  name: mango-email
  namespace: mango
spec:
  type: ClusterIP
  selector:
    app: mango-email
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP