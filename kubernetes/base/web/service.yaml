apiVersion: v1
kind: Service
metadata:
  name: mango-web
  namespace: mango
spec:
  type: NodePort
  selector:
    app: mango-web
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30048
      protocol: TCP