apiVersion: v1
kind: Service
metadata:
  name: mango-reward
  namespace: mango
spec:
  type: ClusterIP
  selector:
    app: mango-reward
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP