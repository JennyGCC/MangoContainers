apiVersion: v1
kind: Service
metadata:
  name: mango-auth
  namespace: mango
spec:
  type: ClusterIP
  selector:
    app: mango-auth
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP